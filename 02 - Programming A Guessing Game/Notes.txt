= When a value is immutable, once a value is bound to a name,
  you can't change that value.

    let x = 5;          // x == 5: immutable x's value

= You can create a mutable variable by adding mut to the var-
  iable name

    let mut x = 5;      // x == 5: mutable x's value